export const environment = {
    production: false,
    apiBase: 'http://localhost:8080/api' // si el front corre en 4200
    // si el front está embebido en Spring (mismo host/puerto), puedes usar apiBase: '/api'
  };
  